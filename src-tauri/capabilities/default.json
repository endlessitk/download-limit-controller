{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the Download Limit Controller window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
